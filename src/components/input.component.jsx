import React from 'react';

export const InputComponent = ({placeholder}) => {
    return(
        
        <input 
            type="input" 
            placeholder={placeholder} 
        />
    );
};